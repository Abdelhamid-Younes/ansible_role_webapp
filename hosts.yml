all:
  vars:
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'

prod:
  hosts:
    client1:
      ansible_user: ubuntu
      ansible_host: 50.17.26.93 # Replace with the actual IP address
      ansible_ssh_private_key_file: ~/.ssh/devops-hamid.pem # Replace with the actual ssh public key of the client machine